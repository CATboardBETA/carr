[package]
name = "carr"
version = "0.1.0"
edition = "2024"
authors = ["CATboardBETA"]

[dependencies]
itertools = "0.15.0"
rand = { version = "0.10.2", optional = true }

[features]
default = []
full = ["rand"]
rand = ["dep:rand"]